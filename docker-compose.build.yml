# Override for building the image locally instead of pulling it:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  minecraft:
    build: .
    image: custom-minecraft:local
